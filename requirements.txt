fastapi>=0.115
uvicorn[standard]>=0.30   # [standard] pulls in websockets — required for the live UI /ws feed
httpx>=0.27       # only for --live real-LLM mode
sqlalchemy>=2.0   # persistence (optional)
asyncpg>=0.29     # persistence (optional)
pgvector>=0.2     # persistence (optional)
